# Pin + CI changes get owner review — CODEOWNERS auto-requests @mousebrains
# (advisory). main is branch-protected (the `validate` check is required +
# enforce_admins) since the engine's SA-teardown retired the snapshot's direct
# push; code-owner review is not set as a hard gate. See kayak_python
# deploy/SETUP.md §2.6.
/dataset.yaml   @mousebrains
/.github/       @mousebrains

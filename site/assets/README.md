# Site Assets

These files are WKCC-owned public site assets. They preserve the social preview,
favicon, and installed-app icons that were formerly packaged in the kayak engine.

The engine treats these as dataset presentation content: if present, build/deploy
copies them over the generic packaged fallbacks.

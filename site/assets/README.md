# Site Assets

These files are WKCC-owned public site assets. They are byte-identical copies of
the social preview, favicon, and installed-app icons currently packaged in the
kayak engine.

A follow-up engine release will treat these as dataset presentation content:
if present, build/deploy will copy them over the generic packaged fallbacks.

# Site Assets

These files are WKCC-owned public site assets. They are byte-identical copies of
the social preview, favicon, and installed-app icons currently deployed for the
WKCC live site.

The kayak engine treats these as dataset presentation content: if present,
build/deploy copies them over the generic packaged fallbacks.

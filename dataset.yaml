# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that requires
# publishable datasets to carry their legal/contact site prose and removes the
# hardcoded Oregon reach-picker default (kayak_python #174).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary), so this
# pin-only change remains compatible with the prior engine: the required
# site/*.md prose files already existed on main.
engine_test_ref: "e8e5e6b46edb71f6614f96b7d8f6de8d4127b3e8"

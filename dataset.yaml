# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine's dataset-separation
# Batch 6 merge (kayak_python #207): R9 deprecation removals (METADATA_DIR alias,
# init-db --no-seed, HC_METADATA_SNAPSHOT allowlist) — the cleanup that completes
# the code/data separation. (Catches the pin up across batches 3-6.)
engine_test_ref: "ba12fa6ed6123da89f70bc371f32ed42e3b21c40"

# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped (S3d-D1) to the engine commit that adds
# the dataset-owned map.yaml model, map config validation, build/fetch-osmb wiring,
# stale-overlay cleanup, and reserved layer-key checks (kayak_python #163, S3d).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary): this is a
# pin-only change with no content edit, so it is byte-clean under the prior engine.
# Once merged, the content PR (S3d-D2) can add the current WKCC/Oregon map.yaml and
# validate it under this engine.
engine_test_ref: "2f57f714fd9bf3ce0a58ed63e42abb71e1d49522"

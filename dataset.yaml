# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that validates
# and installs dataset-owned site/assets over generic packaged public-identity
# fallbacks (kayak_python #173).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary), so this
# change remains compatible with the prior engine: the asset README is metadata
# only, and the asset files already existed on main.
engine_test_ref: "5df1ef48d6efff22f7f5ba51030f4f539bd86919"

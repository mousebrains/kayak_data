# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped (S1) to the engine commit carrying
# `levels generate-sources` (#137), so a follow-up PR can add `generate-sources
# --check` to the dataset CI. The CI reads this pin from the BASE commit (S4b-2
# trust boundary), so the pin must land here before that --check step can pass.
engine_test_ref: "8ce4b18c46058a0c656448c2b602f8a8b9635831"

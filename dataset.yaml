# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that adds the
# generic map-layer fetch alias and MAP_LAYERS_DIR compatibility path
# (kayak_python #180).
engine_test_ref: "13c3361432874b6ce72f58084a3a40124ce5e15d"

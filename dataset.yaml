# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped (S4b-2) to current engine `main`,
# which carries the S6.3 retired-id + S6.4 contract validator that the dataset CI
# now runs (`levels validate-dataset`); the prior #130/S6.2 pin predated it.
engine_test_ref: "aa12ca89982bb2ea7da859ee2eefde0d8ae4da8e"

# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that shares the
# gauge metadata cache default across maintenance scripts while preserving the
# live default (kayak_python #183).
engine_test_ref: "3d1796a2899261777a101392502a499336b4c914"

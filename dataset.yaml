# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped (S3i-D3) to the engine commit that
# removes WKCC site identity defaults from the engine after this dataset added
# site.yaml (kayak_python #166, S3i generic site defaults).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary): this is a
# pin/comment-only change with no dataset content edit, so it is byte-clean under
# the prior engine.
# Once merged, later content PRs validate against the generic site-default
# engine while WKCC site identity remains dataset-owned in site.yaml.
engine_test_ref: "53731adb2de2b264cd66073bd7ab2364d2eeff3b"

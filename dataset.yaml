# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that makes the
# gauge metadata cache path configurable while preserving the live default
# (kayak_python #182).
engine_test_ref: "892c2a7d913d9f84b14f407449d6c5a7ad61852f"

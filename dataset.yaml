# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to kayak_python main @22bfac3
# (#210 + #211): the LI-COR public-dashboard parser + `fetch-licor` POST step
# (the `licor` parser) — required so a follow-up PR can reference
# `parser: licor` and validate against an engine that recognizes it.
engine_test_ref: "22bfac37b5d0fef4f79bea1cead308d0db829994"

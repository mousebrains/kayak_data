# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped (S3i-D2) to the engine commit that
# removes WKCC region defaults from the engine after this dataset added
# region.yaml (kayak_python #165, S3i generic region defaults).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary): this is a
# pin-only change with no content edit, so it is byte-clean under the prior engine.
# Once merged, later content PRs validate against the generic region-default
# engine while WKCC region behavior remains dataset-owned in region.yaml.
engine_test_ref: "58e15f4aa8ffe3ebe00eef849c6083548921e789"

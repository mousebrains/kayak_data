# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that gives the
# engine a generic SITE_URL fallback while requiring publishable deploys to set
# SITE_URL explicitly (kayak_python #175).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary), so the
# accompanying workflow SITE_URL env remains compatible with the prior engine
# while keeping post-merge main validation green under this pin.
engine_test_ref: "92efaaeed1eda29e2fbdd1fbd6b4f5a85e7c7951"

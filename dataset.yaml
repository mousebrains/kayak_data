# Dataset contract manifest — see kayak.dataset.contract in the kayak engine.
contract_version: 1
dataset_id: wkcc
name: WKCC River Levels
status: publishable
license: CC-BY-NC-4.0
# Full 40-hex commit of the approved engine this dataset is validated against.
# S7 formalizes per-release pinning. Bumped to the engine commit that renders
# robots.txt's sitemap URL from SITE_URL instead of a packaged WKCC host
# (kayak_python #168, deploy output unchanged on the live SITE_URL).
# The CI reads this pin from the BASE commit (S4b-2 trust boundary): this is a
# pin-only change with no content edit, so it is byte-clean under the prior engine.
# Once merged, later content PRs validate against the robots-template engine
# while WKCC's live robots.txt output remains controlled by SITE_URL.
engine_test_ref: "859288a7ac8845ad9c53fdf0e4de5a4eb54efc91"
